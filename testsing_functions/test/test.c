#include "../../includes/minishell.h"

int main(int ac, char **av, char **env)
{
	int i = 0;
	int j = 0;	
	char *input;
		input = readline("hello : ");
		if (input[i])
		printf
		("%s",input);
		i++;
	free(input);
}
